# Runtime messages still to translate

Generated by scripts/i18n_todo.py. The numbers are a lower bound of what a user
sees: help text is complete, and the most visible labels, prefixes, hints and
diagnostics are already wrapped or mapped (see docs/I18N.md for the mechanism).

Total Chinese literals in the package: **1336**

## By file

| file | count |
|---|---|
| workflow.py | 280 |
| ops.py | 262 |
| skillspec.py | 136 |
| cli.py | 99 |
| report.py | 98 |
| agentgate.py | 97 |
| bootstrap.py | 88 |
| _collector_remote.py | 49 |
| corrections.py | 44 |
| prov.py | 41 |
| history.py | 38 |
| session.py | 34 |
| mcp.py | 21 |
| preflight.py | 18 |
| collect.py | 16 |
| data.py | 9 |
| yamlmini.py | 5 |
| __main__.py | 1 |

## workflow.py (first 12)

- 返回 {
- :...}}；无文件/损坏 → {}。
- autozt stop 成功后调用：给该步骤打 scancel 标记（auto 不再自动重跑）。
- step_name=None 清整个材料的标记；否则只清本类型该步骤那条。
- 步骤 s 的依赖名列表。skill.yaml 没写 needs 就回退成\
- 返回该技能（任务类型）的并发提交上限；None = 不限。
- 原子占一个槽：未超上限则 +1 返回 True，否则 False。
- %s[%s\|%s]：预生成输入失败：%s
- %s[%s\|%s]：输入已生成（本地待命，等有空位自动提交）。
- gen %s（达 max_jobs 上限，先备输入待提交）
- 下游步骤缺这个数据
- imaginary（声子虚频，动力学不稳定；S6 合理不启动，非 error）

## ops.py (first 12)

- 挂死恢复参数取值优先级：项目 setting.yaml > 技能 task_types > 全局 tf.yaml > 默认。
- 挂死重试计数文件：<配置目录>/.tf_hung.json（workdir → 已恢复次数）。
- 迭代写 stdout
- ALGO=All(新增)
- scancel 后 %ds 作业仍未退出
-   echo 'WARN: CONTCAR 缺失/不完整，保留原 POSCAR 直接重跑'; 
- 默认集群
- hang-check：远端扫描失败（host=%s）：%s
- hang-check：远端扫描输出解析失败（host=%s）：%s
- [%s] hang：job %s（%s）NODE_FAIL 已恢复 %d/%d 次，停止重试。
- [%s] hang[干跑]：job %s（%s）NODE_FAIL，将重跑。
- [%s] hang：job %s 取消失败：%s

## skillspec.py (first 12)

- 加技能友好
- 怎么跑
- 加一个技能
- 技能卡片
- 错误
- 警告
- 问题清单里是否有致命项（autozt schema --strict 据此返回非零）。
- 把问题清单拆成 (errors, warnings)。
- 步骤不存在
- 同 all_steps，但带
- 技能里所有可被引用的步骤标识：name / label / seq（字符串化）。
- 步骤引用是否命中：支持完整名/label/序号，也支持 'step3_PBE_static' 前缀写法。

## cli.py (first 12)

- 整材料级
- --dry-run：打印真实目标对象（复用 _dry_run_steps_for 的语义）。
-   材料 %s  （未能解析）
-   材料 %s  （整材料 %s）
-   材料 %s  %s（无需操作）
- 步骤 %s 
-   材料 %s  步骤 %s [%s]  ->  %s
-   （共 %d 个材料，无任何目标）
- 程序: %s
- 包根: %s（setting/ = 默认模板，skill/ = 技能脚本）
- 程序: %s
-   autozt list      只读总表（不拉取、不提交）

## report.py (first 12)

- 能开火
- 被依赖卡住
- squeue/qstat 给的原因：去外层括号、压掉空白、截断到 limit 个字符。
- 步骤第二行：running → 
- 任务号 (原因)
- 已取消(%s)
- 分组列第二行：有作业显示作业实况；未全完成时指明走到哪个组员。
- 缺
- == %s（%s，%d 个材料）==
- 没有找到任何材料目录。
- （%s: %s 下没有找到材料）
- \nIMAGINARY / DYNAMICALLY UNSTABLE（声子虚频，S6 合理不启动，非错误）:

## agentgate.py (first 12)

- 人敲的
- agent 敲的
- 谁、什么时候、下的什么手
- 命令签名
- 绕过网关就没人知道
- 口头同意
- 命令词
- 当前动作的发起者：AUTOZT_ACTOR 优先；空串 = 不是 agent 会话。
- 破坏性动作要不要令牌。设了 AUTOZT_ACTOR 就默认严格；显式 0/false/off/关 可关。
- 关
- 否
- 批准的命令

## Migration procedure

See docs/I18N.md. Do it in small batches (5-10 messages), wrap the WHOLE
expression, then run: ast.parse -> az skills/schema -> pytest -> i18n_report.
