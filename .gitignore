# ---- 配置 / 密钥（本地专属，不进仓库）----
setting/
setting/.tf_watch.log

# ---- Python 缓存 ----
__pycache__/
*.pyc

# ---- 虚拟环境 / 本地依赖 ----
mace_local_venv/
.pheasy_test_venv/
*_venv/

# ---- 运行时产物 / 测试输出（tf 生成，不进仓库）----
tf_test/**/result/
tf_test/**/log/
test/tf_test/
test/_si_backup/
test/sandbox/
test/unihamgnn/
.tf_fetched
.tf_monitor.out
.tf_monitor.log
queue.out
queue.err
submit.sh

# ---- 备份 / 补丁 / 日志 ----
*.bak
*.bak_*
*.bak-*
*.bak.*
*.log
.patch_trash/
fix_*.patch
fix_*.py
*_unify.patch
apply_fixes.py
taskflow_bandgap_unify.patch

# ---- MACE 模型文件（体积大，用 skill/*/templates/mace/push_model.sh 单独推集群）----
*.model

# ---- 本地临时目录 ----
.fix_bak/
work/
tmp_mace_mu/

# ---- 软著文档 / 输出数据（本地保留，不进仓库）----
rz/
mg_formation_energy_ranking.tsv

# ---- 运行时输出 / 临时产物（本地保留，不进仓库）----
output/
tmp/

# ---- 系统 / 其它 ----
.DS_Store
# ---- 临时/调试残留（含并行 fp13 GPU 修复会话的产物，只忽略不删除）----
scratch_*.py
scratch_*.txt
poll*.sh
fp13_*.slurm
fp_arch.make_3090
DSH_WSL_CRASH_REPORT.md
shengbte_test_summary.md
CONTROL.*
test_launch.sh
fp13_gpu_bug_report.md
fp13_gpu_fix_attempts.md
apply_*.py
rewrite*.py
fix_*.sh
dl_*.sh
processes_local.f90

# 3090 集成测试残留 step.conf（不进仓库）
step.conf.sb3090

# ---- 不再分发：模型权重与赝势（各站点自行准备）----
*.model
*.pt
*.pth
POTCAR
POTCAR.*
# ---- 站点配置：模板照发，真实配置不发 ----
setting/*.yaml
!setting/template-cluster.yaml
# ---- 开发期临时物与配图 ----
output/
hfeshell_tmp/
Codex 图像*.png

# 2026-09-18: 另一会话在 WSL/Windows 路径处理上的残留——目录名本身就是 Windows 路径
    （Windows 路径残留：以 C: 开头、以及以 wsl.localhost 开头），只在 git status 里制造噪声
C\:*
*wsl.localhost*
