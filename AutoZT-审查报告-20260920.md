# AutoZT 代码审查报告

- **日期**：2026-09-20
- **审查范围**：`autozt/` 全部 Python 模块（约 1.7 万行）、`bin/autozt` 入口、`autozt/mcp.py`（MCP 服务）、`autozt/agent_service.py` + `agent_cli.py`（agent 协议层）、`autozt/agentgate.py`（act 网关 + 审计）、`tests/`、`skill/`、AGENTS.md/TASKFLOW.md 文档一致性
- **方法**：静态通读 + 实证复现（复现脚本 + 真实 CLI/MCP 调用 + 逐文件跑测试套件）
- **本次未修改任何代码**，只读 + 写本报告
- **宿主环境**：Windows 侧 Python 3.12 驱动 WSL 里的仓库（`\\wsl.localhost\Ubuntu\...`）。`wsl.exe` 被本机安全策略拦截，**无法在 Linux 侧复跑测试**——下文已逐条标注哪些结论受此限制。

---

## 0. 结论摘要

| # | 项目 | 结论 |
|---|---|---|
| 1 | **CLI 服务** | ✅ 基本可用。`--help` / `skills` / `config` / `act policy` 全部 rc=0，子命令分派正常 |
| 2 | **MCP 服务** | ✅ 端到端可用。stdio JSON-RPC 握手、`tools/list`、`prompts/list`、`resources/list`、`tools/call`、错误路径全部合规 |
| 3 | **MCP 的高层执行路径** | ❌ **有 P0 缺陷**：带作用域的 `cycle(execute=true)` 100% 失效（见 BUG-1） |
| 4 | **远程命令执行层** | ❌ **有 P0 缺陷**：非 UTF-8 输出导致整条命令被判失败且错误信息全丢（见 BUG-2） |
| 5 | **act 审计** | ❌ `autozt act log` 抛未捕获异常（见 BUG-3） |
| 6 | **MCP profile 安全开关** | ❌ **fail-open**：profile 名写错会静默放开到最宽松表面（见 BUG-4） |
| 7 | **运行时污染仓库** | ⚠️ 仓库根出现 9 个垃圾目录，已定位其中 7 个的确切成因（见 BUG-5） |
| 8 | **文档一致性** | ⚠️ AGENTS.md 技能清单漂移；铁律 0 要求的 README 有 8 个技能缺失 |
| 9 | **测试套件** | ⚠️ Windows 宿主上大面积伪失败，其中 3 类是测试自身的问题 |

**修复优先级**：P0 = BUG-1、BUG-2；P1 = BUG-3、BUG-4、BUG-5；P2 = 文档与测试（§5、§6）。

---

## 1. P0｜BUG-1：MCP `cycle` / `run` 带作用域时必然误报「计划过期」，一个动作都不执行

### 现象

`docs/mcp.md` 明确推荐的写法（`cycle` + `tt` + `execute=true`），以及生产中最常见的 `material=xxx` 收窄，全部返回失败：

```
CASE 1: cycle(execute=true) 带作用域 material=Si_demo
  isError           = True
  error             = plan is stale; call get_snapshot or inspect again
  mode              = execute
  stale_plan        = True
  expected_cursor   = 26476d6572898c68
  current_cursor    = cbc250937c7e5843
  实际执行的动作数  = 0
  act 网关是否被调用 = False
```

对照——**同样参数、去掉作用域**就正常：

```
CASE 2: 同样参数但不带作用域
  isError = False   stale_plan = None   act 网关被调用 = True
```

四种作用域对照（`tests` 模拟真实多技能项目：band-dft-cpu 2 个材料 + kl-dft-cpu 1 个材料）：

```
cycle  [无作用域]                    execute=True  stale=None  动作数=3 act调用=3   ← 正常
cycle  [tt=band-dft-cpu]             execute=True  stale=True  动作数=0 act调用=0   ← ★ docs/mcp.md:103 官方示例写法
cycle  [material=Si_a]               execute=True  stale=True  动作数=0 act调用=0   ← ★ 生产中按材料收窄
cycle  [status=ready]                execute=True  stale=None  动作数=3 act调用=3   ← 见下方注
apply_actions 显式带 tt + 正确 cursor  ->  stale=None 动作数=1 act调用=1            ← 同一逻辑的底层调用是好的
```

> 注：`status=ready` 那一行「看似正常」是模拟数据的产物——我的假 CLI 对 `-status` 未改变输出，所以收窄前后状态恰好相同。真实环境下 `-status` 会改变输出，同样会命中此缺陷（机制见下）。

### 根因

`autozt/mcp.py` 的 `_cycle()`（约 631–656 行）内部闭包 `apply()`：

```python
def apply(actions, dry_run, expected_cursor=None):
    apply_args = {"dry_run": dry_run, "actions": actions}
    if not dry_run and expected_cursor:
        apply_args["cursor"] = expected_cursor        # ← 只带了 cursor
    result = _apply_actions(apply_args)               # ← 作用域键 tt/material/step/status 全丢了
```

而 `_apply_actions()`（666 行起）在 CAS 校验时会**用这套 args 重新加载状态**：

```python
expected = args.get("cursor")
if expected:
    compact, error, rc = _mcp_load_compact(args)      # args 里没有 tt/material/step/status
    actual = _protocol.state_cursor(compact or {})
    if str(expected) != actual:                       # 必然不等 → stale_plan
```

`_mcp_load_compact` → `_load_state` → `_state_argv` 会把作用域翻译成 CLI 参数（`mcp.py:220 _mat_args`：`tt→-tt`、`material→-p`、`step→-j`；`mcp.py:390 _state_argv`：再加 `-status`）。

于是形成**两个不同的状态集合、两个不同的 cursor**：

| 阶段 | 代码路径 | 作用域 | 得到的 cursor |
|---|---|---|---|
| 观察/规划 | `agent_service.inspect(load_state, scope, ...)` | ✅ 有 | **收窄态** cursor（data["cursor"]） |
| 执行前 CAS 复检 | `_apply_actions` → `_mcp_load_compact(args)` | ❌ 无 | **全量态** cursor |

cursor 是「状态内容的 sha256」（见 `agent_service.py:341`：`"cursor": "sha256 of the compact scoped state"`），两个集合内容不同 → 校验必然失败 → 判定「计划过期」，**并在这一点上终止，动作一个都不下发**。

### 影响

- 只要同时具备「有作用域」+「execute=true」两个条件，**MCP 高层执行路径 100% 不可用**。
- 这是文档推荐姿势 + 最常用姿势，等于把 AutoZT 的「让 agent 自动推进流水线」这一核心卖点打掉了。
- 「失败方向」是安全的（不会提交作业，不会执行错动作），所以**不会有人被坑到提交错误作业**——但自动化会彻底空转，且错误文案 `plan is stale` 会把排查方向误导到「状态被别人改过」，而真相是代码自己丢参数。
- 同一缺陷同时影响 `cycle` 和 `run`（`agent_service.py:183`，`run` 只是 `cycle` 的别名）。

### 建议修复方向（未实施）

`apply()` 里把作用域键一并传下去，保持两个阶段的加载条件一致：

```python
apply_args = {"dry_run": dry_run, "actions": actions}
apply_args.update({k: args[k] for k in ("tt", "material", "step", "status")
                   if args.get(k)})          # ← 关键补充
if not dry_run and expected_cursor:
    apply_args["cursor"] = expected_cursor
```

或让 `_apply_actions` 接受显式 `scope` 参数，避免「同一个函数既当底层原语、又当高层收窄器」的歧义。

**回归测试建议**：在 `tests/test_mcp.py` 里加一条「带作用域 execute 必须与不带作用域的 cursor 语义隔离」的用例——现有测试没覆盖到，所以这个 bug 能活到现在。

---

## 2. P0｜BUG-2：`run_remote` 严格按 UTF-8 解码子进程输出，命令成功却被判失败、错误信息全丢

### 现象（实证）

```
$ python -c "from autozt import run_remote; rc,out = run_remote({...}, "printf \"GBK字符串\" >&2; exit 0")"

  File "subprocess.py", line 1599, in _readerthread
    buffer.append(fh.read())
UnicodeDecodeError: 'utf-8' codec can't decode byte 0xd2 in position 0: invalid continuation byte
returncode = 1
输出       = ''
```

远端命令 **exit 0（成功）**，`run_remote` 却返回 `rc=1`、输出为空字符串。异常在读取线程里抛出，被上层吞掉，**原始字节一个都没保留**。

### 这不是理论风险——它正在打断本地测试

逐文件跑 `tests/test_*.py` 时，同一个 `0xd2` 反复出现并直接打死三个套件：

| 套件 | 观测到的报错 |
|---|---|
| `suite_autodeps` | `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xd2 in position 0`（2 次），随后 3 项检查连锁失败 |
| `suite_prov` | 同上（2 次） |
| `suite_poscar_sync` | 同上（4 次） |

这些套件都会走 `remote_gen(host=None)` 的「本地 bash 模式」，而本机 bash 的 stderr 输出是非 UTF-8（Windows GBK 控制台）——于是**命令本身可能完全正确，却被判定失败**。

### 影响

- 中文集群上 module/Lmod 的提示、`sbatch`/`qsub` 的中文报错、甚至 VASP 的部分诊断输出都可能带非 UTF-8 字节。一旦命中，`autozt` 表现为「失败，但不说为什么」。
- 这直接破坏铁律 5 的可诊断性（「失败时把输出原文呈给用户」）——因为原文已经被解码异常吃掉了。
- 对 MCP 尤其危险：`_load_state` 一旦 rc≠0，返回的 `error` 是 `""`，agent 收到的是「读取失败 + 空原因」，无法自愈也无法上报。

### 建议修复方向（未实施）

解码不要「要么全对、要么全丢」：

```python
subprocess.run(..., text=True, encoding="utf-8", errors="replace")
# 或 errors="backslashreplace"，至少保留可见的转义痕迹
```

关键点是 **`errors=` 必须显式给**，同时在解码失败时不要丢弃 `stderr`。

---

## 3. P1｜BUG-3：`autozt act log` 直接抛未捕获异常

### 现象

```
$ autozt act log
Traceback (most recent call last):
  File ".../agentgate.py", line 485, in cmd_act
    return agent_render_log(cfg, n=40, json_out=False)
  File ".../agentgate.py", line 411, in agent_render_log
    evs, total = agent_log_load(cfg, proj=proj, since=since)
  File ".../agentgate.py", line 269, in agent_log_load
    for line in f:
UnicodeDecodeError: 'utf-8' codec can't decode byte 0xaa in position 74: invalid start byte
EXIT=1
```

### 两处缺陷叠加

**（a）读侧只接 `OSError`，没接 `UnicodeDecodeError`**（`autozt/agentgate.py:259-291`）：

```python
try:
    with open(path, encoding="utf-8") as f:      # 默认 errors='strict'
        for line in f:                            # ← 一旦有坏字节，异常从这里冒出
            ...
            try:
                e = json.loads(line)              # 单行 JSON 坏了会被 continue 优雅跳过
            except ValueError:
                continue
except OSError:                                   # ← 只兜了 IO 错误
    return [], 0
```

讽刺的是：**单行 JSON 坏掉有优雅兜底（`continue`），整文件字节坏掉反而没有**。写侧 `agent_audit()` 有 `try/except` 并注释「审计失败绝不影响命令本身」，读侧缺这层对称保护。

**（b）审计日志本身已被写坏**。实测 `setting/.tf_agent_log.jsonl`：

- 大小 811,910 字节
- 首个非法字节在偏移 221,258，值 `0xaa`
- 上下文：`... "why": ["只读命令"]}\r\n\xaa\x8b\xe8\xaf\xbb\xe5\x91\xbd\xe4\xbb\xa4"]}\r\n{"ts": "2026-09-...`

把 `\xe5\x8f\xaa\xe8\xaf\xbb\xe5\x91\xbd\xe4\xbb\xa4` 还原就是「只读命令」的 UTF-8 字节。坏行缺了首字节 `e5 8f`，后半截 `\xaa\x8b\xe8\xaf\xbb...` 重复出现一次——这是典型的**撕裂写（torn write）**：多个进程并发以 `"a"` 模式追加同一文件，其中一个写到一半、另一个插了进来。

### 影响

审计日志是 act 网关「谁在什么时候动了什么」的信任基础。现在只要出现一条半行（并发写、磁盘满、进程被强杀都可能），`act log` 就**永久不可用**，而且没有任何提示告诉你「第几行、什么内容坏了」。排查这条 log 的人只会看到一段 Python traceback。

### 建议修复方向（未实施）

1. 读侧改 `open(path, encoding="utf-8", errors="replace")`，或额外 `except UnicodeDecodeError` 后逐行 `open(..., errors="replace")` 重试，并报告坏行行号；
2. 写侧加并发保护（`fcntl.flock` / `O_APPEND` 单次 `write` 完整行），并把 `except OSError` 扩成 `except Exception`——注释里已经承诺「审计失败绝不影响命令本身」，但 `TypeError`（比如 `rec` 里混进不可序列化对象）现在会漏出去。

---

## 4. P1｜BUG-4：MCP profile 名写错 → **静默放开到最宽松**（fail-open）

### 现象

实测各环境变量的工具表：

| 设置 | 暴露工具数 | 是否含变更类工具 |
|---|---|---|
| 默认（不设） | 12 | 否 |
| `AUTOZT_MCP_PROFILE=workflow` | 12 | 否 |
| `AUTOZT_MCP_PROFILE=full` | 24 | 是 |
| `AUTOZT_MCP_PROFILE=readonly` | **24** | **是（含 6 个）** |
| `AUTOZT_MCP_PROFILE=workflwo`（打错字） | **24** | **是** |
| `AUTOZT_MCP_READONLY=1`（正确的只读开关） | 10 | 否 ✅ |

`AUTOZT_MCP_PROFILE=readonly` 暴露的 24 个工具里，含 `cycle`、`apply_actions`、`start_step`、`clean_material`、`rebuild_step`、`cancel_step` 六个会动状态/提作业/删目录的工具。

### 根因

`autozt/mcp.py:843 _profile_tool_names()` 是个没有 `else` 兜底的洞：

```python
profile = (os.environ.get("AUTOZT_MCP_PROFILE") or "workflow").strip().lower()
if profile == "compact":  return COMPACT_TOOLS
if profile == "monitor":  return MONITOR_TOOLS
if profile in ("workflow", "agent"):  return WORKFLOW_TOOLS
return {item[0] for item in TOOLS}        # ← 任何无法识别的值都落到这里 = 全量 24 个
```

「无法识别」的处理是**给最多权限**，而这恰好也是安全相关的开关。

雪上加霜的是：**只读开关是另一个环境变量**（`AUTOZT_MCP_READONLY`，见 `mcp.py:833`）。代码自己的术语里满是 "read-only profile"、`readonly_profile()`，用户极自然地去设 `AUTOZT_MCP_PROFILE=readonly`，结果拿到的是**全量权限**——与意图完全相反，且没有任何告警。

附带影响：`_result()`（`mcp.py:325-337`）判断 profile 是否属于 `("compact","monitor","workflow","agent")` 来决定是否把 payload 塞进 text 通道；名字写错 → 走 `else` → 同一份大 snapshot 在 `content` 和 `structuredContent` 里**发两遍**，token 翻倍。这与 AGENTS.md 的 token 纪律直接冲突。

### 建议修复方向（未实施）

1. 未识别的 profile 应**失败或降级到最严格**（`return COMPACT_TOOLS` / 直接报错退出），绝不落到全量；
2. 读 `AUTOZT_MCP_PROFILE` 时若值为 `readonly` / `read-only` / `ro`，应识别为只读意图（或至少打印一条警告说明「该值无效，正确开关是 AUTOZT_MCP_READONLY」）；
3. 在启动日志里明确回显「已生效的 profile = X，暴露 N 个工具，只读=是/否」，让配置错误看得见。

---

## 5. P1｜BUG-5：运行时在仓库根目录制造垃圾目录（已定位 7/9 个的确切成因）

### 现象

仓库根目录有 9 个不该存在的条目：

```
%SystemDrive%/
C:UserswangchaoAppDataLocalTemptf_autodeps_7_hajihdremoteXstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_irueiyboremoteXstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_jun2ym2kremoteXstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_juu8jol8remoteXstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_juu8jol8remote2Xstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_u6s0sz45remoteXstep1/
C:UserswangchaoAppDataLocalTemptf_autodeps_xjvvccj6remoteXstep1/
wsl.localhost/Ubuntu/home/wangchao/software/AutoZT/docs/figures/fig1_autozt_overview_radial.svg
```

时间戳横跨 2026-09-18 ~ 2026-09-20，与历次测试运行吻合。

### 根因（7 个 `C:Users...` 目录——已完整实证）

链条如下：

1. `tests/suite_autodeps.py:84`：`tmp = tempfile.mkdtemp(prefix="tf_autodeps_")`
   → Windows 宿主上返回 **Windows 绝对路径** `C:\Users\wangchao\AppData\Local\Temp\tf_autodeps_xxxx`
2. `suite_autodeps.py:85`：`work = os.path.join(tmp, "remote", "X", "step1")`
3. `suite_autodeps.py:119`：`remote_gen(cfg2, t2, m2, "step1", host=None)` → 走「本地 bash 模式」
4. `autozt/workflow.py:675`：

   ```python
   line = "mkdir -p %s && cd %s && " % (shlex.quote(step_dir), shlex.quote(step_dir))
   ```

5. `shlex.quote()` 是 **POSIX** 引号器：字符串里没有空格/特殊字符时它**不加引号**，直接返回原样。`C:\Users\wangchao\...` 不含空格，于是**裸着进入 bash**；
6. bash 把反斜杠当转义符吃掉 → 得到 `C:UserswangchaoAppDataLocalTemptf_autodeps_7_hajihdremoteXstep1`，`mkdir -p` 就在**当前工作目录（= 仓库根）**创建了这一个目录。

推理与实测完全吻合：目录名逐字符对得上；目录内 5 个文件（3 个 `*.py` + `provenance.json` + 1 个 `*.jsonl`）正好是 `suite_autodeps` 那棵自造小技能树的产出。

`wsl.localhost/...` 是同类问题的另一种表现：UNC 路径 `\\wsl.localhost\Ubuntu\...` 被当作**相对路径**落盘，于是把仓库里的 `docs/figures/fig1_*.svg` 镜像了一份到 `wsl.localhost/Ubuntu/.../` 下。

### 未能定位的 1 个：`%SystemDrive%/`

- 内容是**货真价实的 Windows 系统缓存文件**：`ProgramData/Microsoft/Windows/Caches/cversions.2.db`（16,384 B）、`{DDF571F2-...}.2.ver0x0000000000000001.db`（662,848 B）、`{6AF0698E-...}.db`（309,256 B）、`{63696B3B-...}.db`（1,240 B）。
- 全仓 grep `SystemDrive` **无任何命中**（代码/脚本/文档里都没写），所以来源在仓库之外——是某个进程把 Windows 环境变量模板 `%SystemDrive%/ProgramData/...` 当成真实路径，在仓库根目录**逐级建目录并写入了真实缓存文件**。`%SystemDrive%` 是字面量（`od -c` 确认就是 `% S y s t e m D r i v e %`），说明该进程运行的环境没有展开 `%VAR%`（bash 不展开 `%VAR%`）。
- **这一条我没有定位到具体调用方，不做猜测。** 但它的存在本身是问题：仓库里躺着一份 Windows 系统缓存副本。

### 风险

- `git check-ignore` 实测：`%SystemDrive%` **未被忽略**（会被 `git add .` 收进仓库）；`wsl.localhost` 已被 `.gitignore:97 *wsl.localhost*` 覆盖 ✅；`C:Users...` 那 7 个因为 Git 认为「路径在仓库之外」而拒绝处理——**但目录实体确实躺在仓库里**，任何非 Git 的打包/备份/扫描工具都会把它们卷进去。
- 更值得担心的是「同一个成因在真实集群上会怎样」：`shlex.quote` 对含空格或 shell 元字符的路径是安全的（它会加引号），所以**真实 Linux 集群上不会出错**。这个 bug 只伤「Windows 宿主 + 本地 bash 模式」，也就是**只在开发机上炸**。但反过来说，它证明「本地 bash 模式」这条路径缺少平台校验，一旦有人在 Windows 上跑真实项目，同样会静默写错目录。

### 建议修复方向（未实施）

1. `remote_gen` 的「本地 bash 模式」在 Windows 宿主上应显式拒绝或改用 `cygpath`/`os-native` 路径，而不是把 Windows 绝对路径喂给 POSIX 引号器；
2. `.gitignore` 增补 `%SystemDrive%`、`C:*` 等条目做兜底；
3. **这 9 个目录建议你人工确认后清理**——我按规则没有动它们（清理仓库根属于需要你确认的操作）。

---

## 6. P2｜文档与测试的不合理处

### 6.1 AGENTS.md 技能清单已漂移

`autozt skills` 实际输出 **20 个启用技能**，而 AGENTS.md 第 21 行写「16 个技能」并只列了 16 个名字：

- AGENTS.md 未列出的 4 个：**`cohp-cogito`**（0.2, 2 步）、**`eph-qe-cpu`**（0.2, 7 步）、**`fc-fit`**（0.1, 1 步）、**`zt-dft-cpu`**（0.1, 1 步）。
- 另外 `skill/` 下还有个 `_template` 目录（不算技能）。

命名也有漂移：本会话注入的 AGENTS.md 快照写的是 `kl-mace-cpu` / `mlff-mace`，磁盘上的 AGENTS.md 写的是 `kl-mlff-cpu` / `mlff`，而 `git status` 显示正在做 `scripts/mace_mu/* → scripts/mlff_mu/*` 的改名。**mace / mlff 两套名字在文档、目录、脚本里混用。**

后果：AGENTS.md 是 agent 的「唯一事实来源」，清单缺 4 个技能意味着 agent 不会知道它们存在，也就不会用它们（`eph-qe-cpu` 是 7 步的大技能）。

### 6.2 铁律 0 要求的 README，8 个技能没有

铁律 0 强制：「执行任何 `autozt -tt <技能> ...` 前，先读 `skill/<技能>/README.md`」。实测缺失 README 的技能：

```
band-dft-cpu、cohp-cogito、ke-dft-cpu、opt-mlff-cpu、opt-mlff-gpu、
phonon-dft-cpu、phonon-mlff-cpu、phonon-mlff-gpu
```

其中 `band-dft-cpu`（能带）、`ke-dft-cpu`（电子热导率）、`phonon-dft-cpu`（声子谱）是 VASP 主流程的高频技能。**规则要求的前置文档不存在**，agent 只能退到铁律括号里的降级路径（METHODOLOGY → TASKFLOW 章节），但那些技能也没有 METHODOLOGY.md。

建议：要么补齐这 8 份 README，要么在 AGENTS.md 里把降级链和「暂无文档时怎么办」写死，别让规则悬空。

### 6.3 测试套件在 Windows 宿主上大面积伪失败（含 3 类测试自身的 bug）

我用 `pytest tests -q --basetemp=...` 的观测结果：

```
...E.E.EEE..E.E.E..EE................E.................................. [ 55%]
...EE.......FFF...F.F..FF.......EEEEEEEEEEEFFF..FE...EE..                [100%]
```

共 **8 个 suite 失败 + 4 个 zt-dft-cpu 用例失败**。逐条归因（区分真 bug 与环境伪失败）：

| 失败项 | 归因 | 性质 |
|---|---|---|
| `suite_asset_lookup`（3 项） | **测试自身 bug**：断言硬编码 `/` 分隔符，如 `p.endswith("step2_bandgap/step2.15_discriminant/discriminant_common.py")`；Windows 下 `p` 用 `\`，必然为 False。**被测函数其实是对的**——打印出来的路径正是期望的那一份 | 测试不可移植，**建议修** |
| `suite_autodeps`（3 项） | BUG-2 的连带（`0xd2` 解码崩） | 真 bug 导致 |
| `suite_prov`（2 项） | BUG-2 的连带 | 真 bug 导致 |
| `suite_poscar_sync` | BUG-2 的连带（4 次 `0xd2`） | 真 bug 导致 |
| `suite_supercell` | `FileNotFoundError: skill/_common/mace/klmlff_common.py`；实际文件在 `skill/_common/mlff/klmlff_common.py`。全仓 grep `_common/mace` **零命中**，所以是**测试里的目录名写错了**（mace/mlff 改名漏改） | 测试 bug，**建议修** |
| `suite_agentgate` | `ModuleNotFoundError: No module named 'termios'`（Unix-only 模块） | 测试在 Windows 跑不了，**注意 CI 覆盖** |
| `suite_session` | 本机安全策略的 safe-delete 无法 trash `\\wsl.localhost\...`（`0x80070002`） | 环境问题，非产品问题 |
| `test_zt_dft_cpu.py`（4 项） | skill 步骤目录在 Windows 侧是**符号链接**，读取报 I/O error；而测试恰恰断言「必须是 symlink 不是副本」，故必然失败 | 环境问题，但说明该测试无法跨平台 |

**重要限制**：上述结论全部来自 Windows 宿主。**在 Linux/WSL 宿主上这些大概率是绿的**，而我没有 WSL 执行权限（`wsl.exe` 被安全策略拦截），所以**无法给出 Linux 侧的绿灯结论**。我的判断依赖「失败原因本身与平台强相关」这一事实，而非直接观测。

**真该修的三条**：`suite_supercell` 的 `mace→mlff` 路径、`suite_asset_lookup` 的硬编码分隔符、`suite_agentgate` 的 termios（如果 Windows 也要跑 CI）。

### 6.4 `test/` 与 `tests/` 两套目录并存

- `tests/`：pytest 套件（26 个 `.py`）
- `test/`：另一批夹具（49 个 yaml、14 个 conf、10 个 tpl）+ 若干 py，`.gitignore` 里还有 `test/tf_test/`、`test/sandbox/` 等专门条目

命名只差一个 `s`，容易误入。另外 `autozt/tfpkg.py` 不存在，这一轮我没查完 `test/` 下是否还有引用旧包名 `tfpkg` 的残留（见「未覆盖项」）。

---

## 7. 已验证**正常**的部分（供对照）

这几项我做了真实调用，不是读代码推测：

### CLI 服务

```
autozt --help      -> rc=0
autozt skills      -> rc=0   （20 个技能，含版本/步骤数/io/flow 检查列）
autozt config      -> rc=0
autozt act policy  -> rc=0
```

参数解析、子命令分派、`--json` 输出格式都没有发现问题。`act` 的风险分级 + 一次性令牌批准机制设计自洽。

### MCP 服务（stdio JSON-RPC 端到端）

```
exit = 0
[id=1] initialize   -> protocolVersion=2025-06-18  server={'name': 'autozt', 'version': '2'}
[id=2] tools/list   -> 12 个: list_skills, capabilities, schema, describe_skill, probe_step,
                              research_plan, preflight, results, inspect, cycle,
                              get_snapshot, apply_actions
[id=3] prompts/list -> triage-failures, review-before-submit, compute-zt,
                       run-validated-workflow, explain-result-provenance
[id=4] resources/list -> 41 个资源
[id=5] call -> ok=True rc=0 isError=False   （capabilities）
[id=6] call -> ok=True rc=0 isError=False   （list_skills）
[id=7] call -> isError=True rc=1 error=unknown tool: descriptor_not_exist      ← 未知工具被正确拒绝
[id=8] call -> isError=True rc=1 error=unknown argument(s): nope               ← 未知参数被正确拒绝
[id=9] JSON-RPC ERROR -32601: method not found: no/such/method                 ← 协议层错误码正确
[id=10] call -> isError=True rc=1 error=argument actions[0].action is not allowed
                (destructive or unsupported)                                    ← 破坏性动作被正确拦截
[id=11] call -> isError=False rc=0 error=None                                  ← 正常调用通过
```

**握手、能力发现、错误语义、破坏性动作拦截全部合规。** MCP 的 cursor / CAS 设计思路本身也是对的——`apply_actions` 在**显式传入作用域 + 正确 cursor** 时工作正常（§1 表格最后一行）。BUG-1 只是 `cycle` 这条「便捷路径」忘了把作用域传下去，不是设计缺陷。

---

## 8. 本次未覆盖 / 无法验证的部分

诚实说明边界，避免这份报告被过度采信：

1. **Linux/WSL 侧的任何结论**——`wsl.exe` 被本机安全策略拦截（`PROGRAM BLOCKED BY SECURITY POLICY`），所有执行都在 Windows 侧 Python 上完成。
2. **真实超算链路**——未连接 jzzn / a800 / 3090，没有验证 `ssh`、`sbatch`、结果回拉。BUG-2 在真实链路中的触发频率只能推断，无法实测。
3. **`skill/` 下 201 个 gen/check 脚本的逻辑正确性**——未逐个审查，只做了资产查找路径（`find_asset`）和技能元数据的一致性核对。
4. **`autozt/ops.py`、`report.py`、`science.py`、`_collector_remote.py` 等模块**——通读为主，未做逐行审查与复现。
5. **`test/` 目录下是否残留旧包名 `tfpkg` 引用**——未查完。
6. **`%SystemDrive%/` 目录的确切来源**——只做了内容取证，未定位到创建它的进程（§5）。
7. **测试套件在 Linux 上的真实通过率**——见 §6.3 的限制说明。

---

## 9. 修复顺序建议

| 优先级 | 项目 | 理由 |
|---|---|---|
| **P0** | BUG-1（`cycle` 作用域）、BUG-2（`run_remote` 解码） | 一个让核心自动化 100% 空转，一个让失败原因不可诊断；都会影响日常使用 |
| **P1** | BUG-3（`act log` 崩溃）、BUG-4（profile fail-open）、BUG-5（仓库污染） | 审计可用性、安全开关方向错误、以及会持续恶化的仓库污染 |
| **P2** | AGENTS.md 技能清单（+4）、8 份缺失 README、测试可移植性（3 条真 bug）、`test/` vs `tests/` 整理 | 不影响运行，但直接影响 agent 的决策质量——AGENTS.md 缺 4 个技能意味着 agent 会漏掉它们 |

**BUG-1 的回归测试**是本次最值得补的：它是一行漏传参数的 bug，如果有一句「带作用域 execute 必须成功」的用例就早就被拦住了。

---

# 第二轮复查（修复后回归验证 + 新问题扫描）

- **时间**：2026-09-20（用户修复 4 个核心缺陷之后）
- **方法**：重跑全部复现脚本与关键命令做回归验证；对 `autozt/` 做剩余维度的扫描（子进程解码一致性、shell 拼接、流合并副作用、测试侧同步）

## 10. 修复回归验证（4/4 通过）

| 原缺陷 | 验证方式 | 结果 |
|---|---|---|
| BUG-1（`cycle` 作用域 CAS 误报 stale_plan） | 重跑 `autozt_repro_bug_a.py` 与 `autozt_repro_scope2.py` | ✅ `tt` 作用域动作数=2、`material` 作用域动作数=1，act 网关被正确调用，无 stale_plan |
| BUG-2（`run_remote` 非 UTF-8 解码崩溃） | GBK 字节经 stderr 的最小复现 | ✅ 返回码与输出均保留（替换字符），不再抛异常 |
| BUG-3（`act log` 未捕获异常） | 对真实损坏的 `setting/.tf_agent_log.jsonl` 跑 `autozt act log` | ✅ 3640 行正常读出，rc=0 |
| BUG-4（profile fail-open） | `AUTOZT_MCP_PROFILE=workflwo/readonly/ro/full` 四种取值实测 | ✅ 打错字降级到 7 个只读工具，`readonly/ro` 映射只读集，fail-closed 生效 |
| 附带 | `tests/test_mcp.py` + `tests/test_agent_cli.py` | ✅ 60 项全部通过（用户侧），我侧复跑关键复现一致 |

修过的 `run_remote`（collect.py:359）两条分支（`bash -s` 与 `bash -c`）都带上了 `errors="replace"` ✅。mcp.py:213、agent_cli.py:131 也本就有 `errors="replace"` ✅。

## 11. 新发现问题

### N1｜P1：同类严格解码还有 12 处残余——包括 `autozt summary` 的主采集路径

BUG-2 只修了 `run_remote` 一个函数，但 `autozt/` 里还有 12 处 `text=True` **不带** `encoding/errors` 的子进程调用。文本模式下子进程输出含任何非 UTF-8 字节（GBK 作业名、Latin-1 路径、scp 警告）就会抛 `UnicodeDecodeError`。按风险排序：

| 位置 | 内容 | 风险 |
|---|---|---|
| `collect.py:340` | **主采集器**（`autozt summary` 每轮必经的入口） | **最高**：远端任何坏字节 → 整个 summary 崩、监控循环断 |
| `_collector_remote.py:640` | 远端 `squeue -o '%i|%j|%T|...'`，**`%j` 是用户可控的作业名** | **高**：别人给作业起个中文名（GBK 编码）就能打死你的采集器 |
| `ops.py:102` / `ops.py:184` | 本机 `squeue` / `sacct`（WorkDir 字段可含非 ASCII 路径） | 中 |
| `workflow.py:864`（`_sh` 通用 helper）、`1304`（远端输入清单）、`1330`（HanHai connector 输出） | 中 | |
| `workflow.py:1456`、`2027`、`prov.py:229` | tar 解包 stderr | 低（仅 tar 报错时触发） |
| `ops.py:1895` | `crontab -l` | 低 |

**建议**：与 `run_remote` 同款处理——统一加 `encoding="utf-8", errors="replace"`。优先级最高的是 `collect.py:340` 与 `_collector_remote.py:640`。

### N2｜P2：`run_remote` 把 stdout 和 stderr 拼成一个字符串返回，存在静默失效面

修复后的 `run_remote` 返回 `(r.stdout or "") + (r.stderr or "")`（collect.py:380）。两个副作用：

1. **`ops.py:272-276`**：对返回值直接 `json.loads`（有 `except ValueError: changed = []` 兜底）。ssh 一旦往 stderr 打一条警告（首次连接的 host key 提示、ControlMaster 提示），合法 JSON 前后混入非 JSON 文本 → **解析失败被吞，`changed` 静默变 `[]`**——改配置的操作"成功"但实际什么都没改，且无任何告警。
2. **`workflow.py:1198`**：用 `"MARKER_OK" in (o or "")` 判产出。正常路径没问题，但流合并后 stderr 内容理论上可造成误判（低风险，需远端 stderr 恰好含该串）。

**建议**：`run_remote` 返回值改为三元组 `(rc, stdout, stderr)` 或至少给合并串加流标记；`ops.py:274` 的解析失败应打警告而不是静默 `[]`。

### N3｜P3：三处 shell 拼接未加引号（内部数据源，注入面低，建议统一）

- `ops.py:283`：`scancel %s % jobid`——jobid 来自采集状态，正常是数字，但未 quote。
- `ops.py:289`：`squeue -h -j %s` 同上。
- `collect.py:366`：`export PATH="%s:$PATH"`——`remote_path_prefix` 配置值放进双引号，若含 `"`/`$` 会被 shell 展开。配置可信所以低风险。

对照：`workflow.py:1195-1196` 已正确使用 `shlex.quote` ✅。建议三处补齐保持一致。

### N4｜测试侧：`suite_poscar_sync` 仍抛 `0xd2` 解码异常——是**测试自己的** run() helper

栈定位到 `tests/suite_poscar_sync.py` 的内部帮助函数（`subprocess.run(..., text=True)` 无 `errors=`）——产品侧已修，测试代码没同步。**修测试即可，非产品问题**。

## 12. 第二轮结论

- **4 项核心修复全部验证通过，质量良好**（含 fail-closed 的方向性修正与并发审计写入）。
- **最值得马上做的下一步**：N1 的 `collect.py:340` + `_collector_remote.py:640`——这是 `autozt summary` 的咽喉，与刚修好的 BUG-2 完全同构，任何一个集群出现非 UTF-8 作业名就会重演"监控全断"。
- 其余（N2 流合并语义、N3 引号一致性、N4 测试同步）可以排在下一批。

---

# 第三轮复查（2026-09-21，N1/N4 修复后回归 + 新维度扫描）

## 13. 修复回归验证（N1/N4 通过）

- **N1（12 处严格解码残余）**：逐点确认全部带上 `encoding="utf-8", errors="replace"` ✅
  - `collect.py:340`（主采集器）、`_collector_remote.py:640`（远端 squeue `%j`）、`ops.py:102/185/1897`、`workflow.py:864/1306/1332/1458/2030`、`prov.py:229`、`session.py:77` 及 `run_remote` 两条分支、`mcp.py:213`、`agent_cli.py:131`——产品侧 17 处 `text=True` 调用点全部覆盖。
- **N4（`suite_poscar_sync` 测试 helper）**：已加 `errors="replace"` ✅（tests/suite_poscar_sync.py:46）。
- 用户侧 `test_mcp.py + test_agent_cli.py + suite_poscar_sync.py` 62 项全过——**我侧无法复跑 bash 相关用例**（见 §15 限制），代码级验证与用户结论一致。

## 14. 第三轮新发现

### N5｜P2：AGENTS.md 技能清单漂移仍在，且改名后数量仍未更正

- 仍写「16 个技能」（第 21、41、69 行共 3 处），实际 **20 个**（`_template` 除外）。
- 技能目录已从 `*-mace-*` 改名为 `*-mlff-*`（`kl-mlff-cpu`/`opt-mlff-cpu`/`phonon-mlff-cpu` 等，`mlff-mace`→`mlff`）——AGENTS.md 的名字已同步 ✅，但数量没改，且漏列 `cohp-cogito`/`eph-qe-cpu`/`fc-fit`/`zt-dft-cpu`。
- **8 个技能仍无 README**：`band-dft-cpu`、`cohp-cogito`、`ke-dft-cpu`、`opt-mlff-cpu`、`opt-mlff-gpu`、`phonon-dft-cpu`、`phonon-mlff-cpu`、`phonon-mlff-gpu`。铁律 0 要求「动手前先读 `skill/<技能>/README.md`」——这些技能被操作时前置文档悬空，agent 只能退到 TASKFLOW.md。

### N6｜P3：三个测试自身的问题仍未修（其一在 Linux 上也必然挂）

| 套件 | 问题 | 影响面 |
|---|---|---|
| `suite_supercell.py:40` | 加载 `skill/_common/mace/klmlff_common.py`——该目录已改名为 `_common/mlff/`（第 37 行 sys.path 用的是新名，第 40 行却是旧名，**自相矛盾**） | **Linux 上也必然 FileNotFoundError**，不只是 Windows |
| `suite_asset_lookup.py:52` | `p.replace(ROOT + "/", "")` 硬编码 `/`，Windows 下 ROOT 是反斜杠永不匹配 | 仅 Windows |
| `suite_agentgate.py` | 依赖 Unix-only `termios` | 仅 Windows |

### N7｜P3：9 处文本模式 `open()` 未指定 encoding（多为无害，2 处建议补）

`ops.py` 5 处、`workflow.py` 3 处、`_collector_remote.py` 1 处。其中 pidfile 读写、`os.devnull` 无害；`workflow.py:64`/`76`（marks json 的读/写对）与 `1970`（FETCH_STAMP 读取）在 Windows GBK locale 下会产生平台相关编码文件——`json.dump(..., ensure_ascii=False)` 含中文时 Windows 写 GBK、Linux 写 UTF-8，跨平台共享目录（如 SMB/OneDrive 同步项目目录）会出现乱码。P3 卫生项。

### N8｜备注：`history.jsonl` 追加写无锁，但读侧已容错

`history.py` 事件追加是逐行 `write()` 无锁（与 agentgate 修复前同类）；但读侧 `except ValueError: continue` 跳过坏行、状态快照走 tmp+`os.replace` 原子写 ✅。并发撕裂最坏情况是丢一行事件，不会崩——可接受，仅备注。`agentgate` 新加的 `_AUDIT_WRITE_LOCK` 模式如需严格可照搬到此处。

### 第三轮澄清（非产品问题）

- `suite_session` 的「agent 审计计数 12/6 ≠ 2/1」：是本沙箱 safe-delete 守卫拦截了 `shutil.rmtree(tmp/_session_test)`，旧审计日志跨 6 次重跑累积（12 = 6×2）。**沙箱伪影**。
- `suite_autodeps`/`suite_prov`/`suite_poscar_sync` 在我侧的失败：本沙箱 Python→bash 通路整体损坏（`subprocess.run(['bash','-c','true'])` 都返回 rc=1 加乱码；直接 bash 正常）。**沙箱伪影**，用户侧 62 项全过可信。

## 15. 第三轮结论

- **N1/N4 修复完整、方向正确**，产品侧 17 处子进程文本解码点已无死角。
- 剩余待办收敛为：**N5**（AGENTS.md 数字 + 8 份 README，影响 agent 决策质量，建议优先）、**N6**（`suite_supercell` 在 Linux 也挂，应排在这批测试修复首位）、N7/N8（卫生项）、以及用户已声明暂缓的 N2/N3。
- BUG-5（仓库根 9 个垃圾目录）仍未清理，等你确认后处理。

---

# 第四轮复查（2026-09-21，N5/N6/N7 修复后回归）

## 16. 修复回归验证（通过，2 个小尾巴）

| 项 | 验证结果 |
|---|---|
| N6 `suite_supercell` | ✅ 本地实跑 `PASS 26, FAIL 0`（此前 Linux/Windows 都挂的 FileNotFoundError 已消除） |
| N7 `workflow.py` 状态文件 | ✅ `_scancel_load`（:64，读）带 `encoding+errors`；`_scancel_save`（:76，写）带 `encoding="utf-8"`；`_fetch_receipt_valid`（:1970，读）带 `encoding+errors` |
| N5 AGENTS.md | ✅ 三处「16 个技能」全部改为 20，清单与 `autozt skills` 实际输出**逐一吻合**（20 个技能名完全一致），补齐了 `zt-dft-cpu`/`cohp-cogito`/`eph-qe-cpu`/`fc-fit` 及 mlff 改名 |

**两个小尾巴**：

1. **TASKFLOW.md 仍有 2 处「16 个技能」残留**（第 18 行目录项、第 28 行概述；第 77 行已是正确的 20）。同一类文档漂移，改 AGENTS.md 时漏了姊妹文件。
2. `_fetch_receipt_write`（workflow.py:1984）的 `NamedTemporaryFile(mode="w", ...)` **写侧未带 `encoding="utf-8"`**——读侧已修，写侧漏了。实际影响很小（receipt 字段是 host/dir/files 路径，通常 ASCII；极端情况下 receipt 判无效只多拉一次文件），但既然读侧按 UTF-8 校验，写侧应一致。

## 17. 第四轮结论

- 累计四轮：**P0/P1 全部清零并验证**；P2 剩 TASKFLOW.md 的 2 处数字残留；P3 剩 `_fetch_receipt_write` 一行、8 份技能 README（用户已知悉，回退链指向 TASKFLOW.md）、以及声明暂缓的 N2/N3。
- 仓库根垃圾目录：`wsl.localhost/` 已消失（或已清理），`%SystemDrive%/` 与 7 个 `C:Users...tf_autodeps_*` 仍在，等确认后处理（BUG-5）。
- 代码审查到此可以收口：剩余项均为低风险卫生项，不再有影响可用性/正确性/安全的问题。

---

# 第五轮复查（2026-09-21，收尾项修复确认 + 收口）

## 18. 收尾修复确认（全部通过）

| 项 | 验证结果 |
|---|---|
| TASKFLOW.md「16 个技能」残留 | ✅ 已清（grep 全仓库 `.md` 无残留，排除本报告历史记录） |
| `_fetch_receipt_write` encoding | ✅ 已加 `encoding="utf-8"`（workflow.py:1985），读写两侧一致 |

## 19. 收口扫描（全维度，无新增问题）

1. **子进程解码**：AST 跨行精确核验（非 grep 单行）确认 `text=True` 的调用点**零遗漏**——全部带 `errors="replace"` 或 `encoding+errors`。
2. **文件编码**：剩余 6 处无 `encoding` 的 `open()` 全部是无害项——`ops.py:1867/1907`（pidfile 写）、`ops.py:1826`（pidfile 读，纯数字）、`ops.py:685/622`（`os.devnull`）、`_collector_remote.py:604`（已带 `errors="ignore"`）。
3. **文档**：AGENTS.md + TASKFLOW.md 技能数一致（20），清单与 `autozt skills` 实际输出逐一吻合。

## 20. 最终结论

- **累计五轮，P0/P1 全部清零并逐项回归验证**；P2/P3 收敛为已知、可接受的尾项。
- **剩余仅 3 项非阻塞**：① 8 份技能 README（用户已知悉，铁律回退链指向 TASKFLOW.md）；② 声明暂缓的 N2/N3（run_remote 返回接口、shell 拼接引号）；③ BUG-5 仓库根垃圾目录（`%SystemDrive%/` + 7 个 `C:Users...tf_autodeps_*`，等确认后清理）。
- **审查可以正式收口**：CLI/MCP 服务与核心执行路径（采集、远程命令、审计、配置、作用域 CAS）均健康，不再有影响可用性/正确性/安全的问题。

---

*报告结束。本次审查全程未修改任何源码，仅新增本报告文件（含二、三、四、五轮复查章节）。*
