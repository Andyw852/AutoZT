# 2026-09-21

## AutoZT 第三轮复查（N1/N4 修复后回归 + 新维度扫描）

结果已追加到 `AutoZT-审查报告-20260920.md` §13-15。

### 验证通过
- N1：产品侧 17 处 `text=True` 子进程调用点全部带 `errors="replace"`（逐点确认 collect/_collector_remote/ops/workflow/prov/session/mcp/agent_cli）。
- N4：tests/suite_poscar_sync.py:46 helper 已修。

### 第三轮新发现
- **N5（P2）**：AGENTS.md 仍写「16 个技能」（21/41/69 行 3 处），实际 20 个；技能已改名 mace→mlff（kl-mlff-cpu 等），名字已同步但数量未改，漏列 cohp-cogito/eph-qe-cpu/fc-fit/zt-dft-cpu；8 个技能仍无 README（band/ke/phonon/opt-mlff 等）→ 铁律 0 前置文档悬空。
- **N6（P3）**：3 个测试自身问题未修——`suite_supercell.py:40` 引用已改名的 `_common/mace/`（37 行用新名 40 行用旧名，**Linux 上也必挂**，应优先修）；`suite_asset_lookup.py:52` 硬编码 `/`（仅 Windows）；`suite_agentgate` 依赖 termios（仅 Windows）。
- **N7（P3）**：9 处文本 open() 无 encoding，多数无害；workflow.py:64/76（marks json 读写对）与 1970（FETCH_STAMP）建议补。
- **N8（备注）**：history.jsonl 追加写无锁，但读侧容错坏行 + 快照 tmp+replace 原子写，可接受。

### 沙箱伪影确认（勿误判为产品 bug）
- 本沙箱 Python→bash 通路全坏：`subprocess.run(['bash','-c','true'])` 都 rc=1 加乱码；直接 bash 正常。凡套件里 `bash -c` 的失败都归此类。
- safe-delete 守卫拦截 rmtree → suite_session 审计计数累积（12/6 = 6 次 × 2/1）。
- 用户侧 62 项测试全过可信。

### 挂起项
- N2（run_remote 返回接口）、N3（shell 拼接引号）用户声明暂缓。
- BUG-5（仓库根 9 个垃圾目录）未清理，等用户确认。

## 第四轮复查（用户修 N5/N6/N7 之后）

- 验证通过：suite_supercell 本地 PASS 26/0；workflow.py scancel 标记读写 + fetch receipt 读侧都带 encoding；AGENTS.md 三处 16→20，清单与 `autozt skills` 实际 20 个逐一吻合。
- **新小尾巴 2 个**：① TASKFLOW.md 仍有 2 处「16 个技能」残留（第 18/28 行，第 77 行已对）；② `_fetch_receipt_write`（workflow.py:1984）NamedTemporaryFile(mode="w") 写侧漏了 encoding="utf-8"（读侧已修，影响仅限极端情况下多拉一次）。
- 仓库根 `wsl.localhost/` 已消失；`%SystemDrive%/` + 7 个 `C:Users...tf_autodeps_*` 仍在（BUG-5 等确认）。
- 收口结论：P0/P1 全清零，剩余均为 P2/P3 卫生项，报告 §16-17。

## 第五轮（收尾确认 + 收口）

- 收尾修复确认：TASKFLOW.md「16 个技能」残留已清（全仓 .md 无残留）；`_fetch_receipt_write` 已加 encoding="utf-8"。
- 收口扫描（全干净）：AST 跨行核验 `text=True` 调用点零遗漏（全部带 errors）；剩余 6 处无 encoding 的 open 全是 pidfile/os.devnull 无害项；AGENTS.md + TASKFLOW.md 技能数一致（20）。
- **审查正式收口**：五轮累计 P0/P1 全清零并回归验证。剩余 3 项非阻塞：8 份技能 README（用户有意推迟）、N2/N3（用户暂缓）、BUG-5 垃圾目录（等确认清理）。报告 §18-20。
